class Solution:
    def getMinMax(self, arr):
        min=float('inf')
        max=0
        for x in arr:
            if min > x:
                min=x
        for x in arr:
            if max < x:
                max=x
        return [min,max]

